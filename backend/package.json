{
  "name": "insightstream-backend",
  "version": "1.0.0",
  "description": "Backend for InsightStream News Aggregator",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
